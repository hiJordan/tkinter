from tkinter import *
'''
def pop(event):
    menus.post(event.x_root, event.y_root)
items = [1, 2, 3]
root = Tk()
menus = Menu(root)
for item in items:
    menus.add_command(label=item)
root.bind('<Button-1>', pop)
root.mainloop()
'''


l = Label(None, text='hi')
l.pack()
l.mainloop()
